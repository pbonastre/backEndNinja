spring:
  datasource:
    url:  jdbc:h2:tcp://172.20.12.48:8082/test
    username: SA
    password: 
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprodNamingStrategy
    properties:
      hibernate:
      dialect: org.hibernate.dialect.H2Dialect